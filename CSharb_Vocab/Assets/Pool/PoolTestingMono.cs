using System;
using System.Collections.Generic;
using UnityEngine;

public class PoolTestingMono : MonoBehaviour
{
    public MonoBehaviour[] monoList;
    public Dictionary<Type, object> MonoToPrefab = new Dictionary<Type, object>();

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
